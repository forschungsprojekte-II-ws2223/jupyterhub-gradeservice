# Gradeservice

## Setup

1. Open up a terminal window. Install dependencies with:

   ```sh
   pip install -e ".[dev]"
   ```

## Adding dependencies

**TODO**
...

## Run

**TODO**
Run the api with:

```sh
uvicorn src:app --reload
```
